import React from 'react';

function Auth(wrapper) {
    return class extends React.Component {
        constructor(props) {
            super(props);
        }
        componentDidMount() {
            
        }
    }
}